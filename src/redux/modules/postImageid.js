const initialState = {
  randomId: crypto.randomUUID(),
};
const postImageid = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default postImageid;
